## This is my first blog post

For this week, making staging area is simple. Used google doc for it. Although it mention that I don't really need to do a google
document, I still do so because it's easy to make. But for what I want to accomplish for this week, is probably understanding
more about the GitHub and the VC code because the one command I had struggle with, is git pull origin command. I'll complain,
this was a annoying. Reason, if you don't check using git commit -v, you'll miss the fetch of which GitHub repository you are
using for VC code. Very annoying, so always check you're in the right fetch before git pull origin command.

 ```tsql
 SELECT *
 FROM sys.tables
 WHERE [name] = 'SomeTable'
 ```
